package com.example.demo.newpackage.oop.polymerphism;

public interface InterestTransactionService {
	Double calculateInterest();
	void processInterest(SavingAccount accountNumber);
	void processInterest();
}